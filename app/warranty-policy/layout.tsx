import type { Metadata } from "next";

export const metadata: Metadata = { alternates: { canonical: "/warranty-policy" } };

export default function WarrantyPolicyLayout({ children }: { children: React.ReactNode }) { return children; }
