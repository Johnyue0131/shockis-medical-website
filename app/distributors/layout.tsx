import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Become a SHOCKIS Medical Distributor",
  description: "Explore SHOCKIS distributor cooperation opportunities for professional shockwave therapy systems.",
  alternates: { canonical: "/distributors" },
};

export default function DistributorsLayout({ children }: { children: React.ReactNode }) { return children; }
